name: xarray-example
channels:
  - conda-forge
  - rapidsai
dependencies:
  - python>=3.12
  - xarray
  - netCDF4
  - bottleneck
  - numpy
  - cupy-xarray
  - ipykernel # needed on brev to enable kernel
  # - matplotlib
  # - cartopy
